# 2026-09-07

### Sesión 4 · 08:59 — Fase A del plan de pulido: módulo profesor (milestone 1)

## Contexto
El usuario aprobó ejecutar la **Fase A** del `PLAN_PULIDO.md` ("vamos con la fase A"). El módulo training existía del lado del profe (`/entrenamiento`) con planes/rutinas/chat básicos (prompts), pero no había vista del alumno, los planes eran solo texto y `due_on` no se usaba. Se relevó el estado completo del módulo (layout, páginas, `TrainingShell`, schema 00002, zona del profe).

## Decisiones
- Ejecutar Fase A en **milestones**: este milestone cubre A1 (vista alumno), A2 (planes estructurados, sin plantillas), A3 (rutinas con fecha + historial, sin calendario ni gráfico) y parte de A4 (marcado de leídos; falta badge visible + adjuntos). Queda pendiente A5 (vinculación bidireccional + disponibilidad) para la próxima sesión.
- Nueva ruta `/mi-entrenamiento` bajo `(social)` (la barra social del alumno), con card naranja "Mi entrenamiento" en `/perfil` (rol alumno); reemplaza al inexistente alias para ese rol.
- **Migración 00013** `trainer_plan_items` (estructura día/ejercicio/series/reps/descanso/nota/posición) con RLS espejo de `trainer_plans` (select involucrados vía join a plan, profe full). `trainer_routines.completed_at` para el historial. Policy `Messages: marca leido` (UPDATE por recipient). Realtime: `trainer_plans`, `trainer_plan_items`, `trainer_routines`, `messages`.
- En todas las vistas realtime se usa `filter: recipient_id=eq.<userId>` (entrantes → reload; los enviados se agregan localmente). El filtro viejo `recipient_id=X,sender_id=Y` era AND y nunca matcheaba → corregido.
- Badge de vencimiento compartido en `src/components/training/DueBadge.tsx` (Vencida/Hoy/fecha) para profe y alumno.
- La app **degrada con gracia** si 00013 no se corrió (queries a `trainer_plan_items`/`completed_at` devuelven error ignorado, no crashean): se puede deployar el frontend antes de la migración.
- Commitear todo el árbol pendiente en un solo commit (el usuario eligió "todo en uno": milestone + buscador/alta de personas + dar salida + panel del gym en perfil público).

## Implementado
- **Vista del alumno `/mi-entrenamiento`**: profes vinculados (`trainer_students` activos), selector si hay varios, y por profe: Planes (estructurados expandibles agrupados por día), Rutinas (pendientes con badge Vencida/Hoy/fecha, banner "N rutinas para hoy", completadas con fecha de `completed_at` → historial), Chat (realtime + auto-leído).
- **Profesor `/entrenamiento/alumno/[id]`**: formularios inline en vez de `window.prompt` (crear plan con título/tipo/descripción; agregar ejercicio con día/series/reps/descanso/nota por plan; rutina con `<input type=date>`), borrar planes/items/rutinas, toggle de completada con `completed_at`, chat con leídos.
- Helpers `todayLocal()` y `formatDay()` en `src/lib/format.ts`.
- Card "Mi entrenamiento" en `/perfil` (rol alumno).
- **Migración `supabase/migrations/00013_training_estructurado.sql`** (PENDIENTE de correr por el usuario en SQL Editor).
- `npm run lint` (0 errores; 3 warnings preexistentes de `<img>`) y `npm run build` OK. Compilado local y en Vercel.
- Checkboxes actualizados en `PLAN_PULIDO.md` (marcados A1/A2 salvo plantillas, A3 salvo calendario/gráfico, A4 parcial; A5 pendiente).
- AGENTS.md: sección Fase A, migraciones (00013 pendiente) y nota de deploy actualizadas.
- Commit `8eb38a5`, deployado a **https://spotterx-five.vercel.app** (32s, alias `spotterx-ieincs6ga-pump13`).

## Pendiente
- ~~Correr migración 00013~~ ✅ **Corrida** (07/09/2026) por el usuario en SQL Editor; AGENTS.md actualizado.
- Probar el flujo en producción (profe crea plan con estructura; alumno ve planes/rutinas/chat).
- Correr migración **00012** (para el botón "➕ Profesor" del buscador de personas) ✅ **Corrida** (07/09/2026) por el usuario.
- Correr **migración 00014** (plantillas de planes: `is_template`).
- Continuar Fase A: plantillas reutilizables (A2), calendario de rutinas + gráfico de historial (A3), badge de no-leídos y adjuntos en chat (A4), y A5 (vinculación bidireccional profe↔gym + mapa + disponibilidad).
- Deployar edge function `delete-account` (pendiente previo del plan).