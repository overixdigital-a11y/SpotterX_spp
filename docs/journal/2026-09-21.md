# Journal — 21/09/2026

## Contexto
- Varios lotes previos (9 a 12) trataron el feed cortado y el badge de diagnóstico en el celular; la caché quedó cerrada y el fix real aplicado (Lote 11).
- Con el feed resuelto, el usuario pidió una mejora de perfil en `src/app/(social)/perfil/page.tsx`: en la sección de accesos del alumno (donde están "Mi gimnasio" y "Marketplace"), agregar **"Ver a mi profesor"**.

## Decisión (aclarada por el usuario)
- "«Ver a mi profesor»" significa **ver las rutinas y planes que el profe le manda** → destino `/mi-entrenamiento` (donde ya vive esa vista con la tarjeta del profe).
- Además, un **botón secundario** al **perfil público del profe** (`/perfil/[username]`), como texto pequeño discreto (sin card, para no duplicar peso visual).

## Implementado (sin migración ni deploy aún)
- **`src/app/(social)/perfil/page.tsx`**:
  - Imports: `useEffect`, `useState`, `createClient`, `GraduationCap`, `UserRound`.
  - Fetch del profe (solo rol `alumno` y con `userId`): `trainer_students` por `student_id` → ids de trainers → `profiles` (username, full_name) → primer profe en estado `profeUsername`/`profeName`. (Patrón ya usado en `mi-entrenamiento`; no se usa `profesor_id`, que no está tipado ni en flujos reales.)
  - **Card "Ver a mi profesor"** → `/mi-entrenamiento` (icono `GraduationCap`, mismo estilo de card que "Mi gimnasio"), visible **siempre para rol alumno**.
  - **Botón secundario "Ver perfil de {profeName}"** → `/perfil/{profeUsername}` (texto pequeño muted, hover neon), visible solo si el fetch encontró el profe. Si el alumno no tiene profe asignado, solo queda el card principal (que lleva a `/mi-entrenamiento`, con su buscador).
- `npm run eslint .` → 0 errores (solo warnings preexistentes de img/Feed/auth-context). `npm run build` → `✓ Compiled successfully` (44 rutas).

## Lote 14 — Experiencia GPS y Mapas Vibrantes Nivel 1
- **Decisión del usuario**: avanzar con el Nivel 1 de mejoras GPS (mapas en modo oscuro, cálculo de distancias reales, pin pulsante de ubicación actual, ordenamiento por cercanía y botones "Cómo llegar").
- **Implementación**:
  - `src/lib/geo.ts`: helper con `haversineDistance`, `formatDistance`, `getDirectionsUrl` y constantes de `DARK_MAP_TILES` (CartoDB Dark Matter).
  - `src/components/gyms/GymMap.tsx`: tiles oscuros, fondo `#0c1017`, popup con enlace "Cómo llegar".
  - `src/app/(social)/mi-entrenamiento/buscar/page.tsx`: tiles oscuros, pin radar `userIcon` para ubicación actual, zoom inteligente en 13, cálculo de distancia por zona y ordenamiento ascendente por cercanía, badges de distancia (`A X km`) y botones "Cómo llegar" en popups y fichas.
  - `src/app/(social)/perfil/[username]/page.tsx`: tiles oscuros en el mapa de sedes del profesor y botones directos "Llegar" en el listado.
- `npm run lint` → 0 errores (7 warnings preexistentes).
- `npm run build` → ✓ Compiled successfully (44 rutas estáticas y dinámicas).

## Lote 15 — Confirmación: integración Git→Vercel activa (deploy automático)
- **Sospecha**: el usuario recordó que había vinculado GitHub con Vercel y preguntó para qué servía (deploy automático vs `vercel --prod` manual).
- **Verificación vía API REST** (token local en `$env:USERPROFILE\AppData\Roaming\xdg.data\com.vercel.cli\auth.json`):
  - `GET /v9/projects/spotterx?teamId=pump13` → `.link` = `{ type: "github", repo: "SpotterX_spp", org: "overixdigital-a11y", productionBranch: "main", gitCredentialId: cr… }` → **vinculación activa**.
  - `GET /v6/deployments?projectId=prj_IR3ARpi5bqwE3Fzjtzd0ZwjsMniT&teamId=pump13` → cada commit push generó deploy automático (`state READY`, `meta.githubCommitSha` OK). El Lote 14 (`46a88e0`) tiene **2 builds** (el automático de la integración + el manual que corrí con `vercel --prod`).
- **Decisión**: el flujo canónico pasa a ser **`git push` a `main` = deploy automático**. No correr `vercel --prod` a mano salvo fallo/no-reflejo tras ~1-2 min.
- **Documentado**: AGENTS.md sección "Deploy / Producción" (reglas + cómo verificar vía API) + flujo de etapas en el Plan Maestro (L68).

## Lote 15b — Fix watermark CartoDB: tiles oscuros Esri sin API key (21/09/2026, commit `db37179`)
- **Problema reportado por el usuario** (en la PC): sobre el mapa aparecía una marca de agua en diagonal **"API KEY REQUIRED"** con link a `carto.com/basemaps/apikey`. Causa: CartoDB ahora exige API key para `basemaps.cartocdn.com/dark_all/...` (cambió su política, antes gratis).
- **Fix**: en `src/lib/geo.ts`, `DARK_MAP_TILES` pasa a **Esri World Dark Gray Canvas** (`{s}.arcgisonline.com/ArcGIS/rest/services/Canvas/World_Dark_Gray_Base/MapServer/tile/{z}/{y}/{x}`, subdomains `abcd`, atribución Esri, maxZoom 19). Servicio público **sin API key**, look dark equivalente. Los 3 consumidores (`GymMap.tsx`, `buscar/page.tsx`, `perfil/[username]/page.tsx`) se arreglan con solo este cambio.
- **Lint** 0 errores; **build** ✓ Compiled.
- **Deploy**: `git push` solo (`46a88e0..db37179`) → integración Git de Vercel generó el build automático (READY) y quedó en producción. **Primera validación real del flujo "push = deploy"**. No se corrió `vercel --prod`.