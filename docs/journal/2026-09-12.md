# Journal — 12/09/2026

## Contexto
- Continuación de la Etapa 5 del plan maestro: Marketplace fit + billetera + panel admin.
- Se completó el Lote 1 (migraciones 00026+00027 + páginas del marketplace).
- Deploy pendiente por token de Vercel expirado.

## Decisiones
- Comisión inicial 1% configurable via `platform_config`, cada orden guarda snapshot.
- Billetera interna con cargas/retiros manuales del admin.
- Categorías 100% deportivas.
- Fotos del marketplace en bucket `media` existente (`<uid>/market/<ts>.<ext>`).

## Implementado
- **Migración `00026_market.sql`**: tablas `market_products`, `market_orders`, `market_order_items`, `market_reviews` + RLS + índices + realtime + RPC `market_checkout` (security definer, FOR UPDATE stock, 0→sold) + trigger `notify_market_order` + constraint `'orden'` en notifications.
- **Migración `00027_wallet_admin.sql`**: `profiles.is_admin`, `platform_config`, `wallets`, `wallet_transactions` + RPCs (`get_or_create_wallet`, `buy_from_wallet`, `admin_set_commission`, `admin_credit_wallet`, `admin_mark_withdrawal_paid`, `request_withdrawal`, `get_my_wallet`).
- **`src/lib/market.ts`**: tipos, categorías, helpers del marketplace.
- **`src/components/market/ProductCard.tsx`**: card de producto.
- **`/market`**: home con búsqueda + chips categoría + grid + "Cargar más".
- **`/market/[id]`**: detalle con galería, vendedor, cantidad, carrito, preguntar→chat, reseñas.
- **`/market/crear`**: form publicar (fotos múltiples, categoría, precio, stock, condición, ubicación).
- **`/market/carrito`**: carrito localStorage + delivery + totales con comisión + checkout wallet.
- **`/market/mis-publicaciones`**: pestañas Publicaciones (status toggle, vendido) + Ventas (estado, marcar entregada).
- **`/market/mis-compras`**: órdenes como comprador (cancelar, ver detalle).
- **`/market/billetera`**: saldo, historial, solicitar retiro.
- Entry points: card Marketplace en `/perfil` (todos los roles).
- Commit `4b0ac8b` + deploy. Migraciones **00026 + 00027 corridas** 12/09/2026.

### Lote 2 — Panel admin (commit `a87fd33`, deployado 12/09/2026)
- **`/market/admin`**: página admin solo para `is_admin`.
  - Dashboard KPIs: productos (total/activos), órdenes (total/mes), ventas del mes, comisión acumulada.
  - Gráfico de ventas últimos meses (recharts BarChart).
  - Categorías populares con barra de progreso.
  - Configuración de comisión (`admin_set_commission` RPC).
  - Acreditar saldo a cualquier usuario (`admin_credit_wallet` RPC).
  - Retiros pendientes con botón "Marcar pagado" (`admin_mark_withdrawal_paid` RPC).
- Entry point: icono Shield Admin en header de `/market` (solo `is_admin`).
- Profile type: agregado `is_admin: boolean`. auth-context: default `is_admin=false`.
- Build OK (0 errores, 3 warnings preexistentes).

### Lote 3 — Panel global admin (commit `6808347`, deployado 12/09/2026)

**Fix crítico**: `(market)/layout.tsx` ahora monta `AuthProvider` — desbloquea todas las páginas del market + `/market/admin`.

**Migración `00028_admin.sql`** **CORRIDA** 12/09/2026:
- `profiles.is_banned boolean default false`
- `post_reports`: `status` (pending/resolved/dismissed) + `resolved_by`
- RPCs `security definer` con `_assert_admin`: `admin_global_stats`, `admin_set_verified`, `admin_toggle_ban`, `admin_delete_post`, `admin_resolve_report`, `admin_delete_exercise`, `admin_delete_food`

**Panel `/admin`** (nuevo route group `(admin)` + AdminShell con nav tabs):
- Dashboard: KPIs globales (usuarios por rol, gyms, profes, posts, órdenes, ventas, comisiones)
- Usuarios: búsqueda + verificar + banear + ver perfil
- Moderación: reportes pendientes → eliminar post / descartar
- Gyms: lista con ciudad + miembros
- Profes: lista + cantidad de alumnos
- Catálogo: ejercicios + alimentos (buscar + eliminar)
- Market: ventas + comisiones + exportar CSV

**Ban a nivel app**:
- auth-context: `is_banned` → signOut + redirige a `/login?banned=1`
- Login: muestra "Tu cuenta ha sido suspendida"
- Profile type: `is_banned: boolean`

**Edge function `invite-member`** extendida:
- `users[]` batch para importación CSV
- `as_admin` flag para que el admin importe en cualquier gym
- Compatible con interfaz single existente

**Importación CSV** en `/gimnasio/miembros`:
- Botón "Importar desde CSV" → parse (nombre, email, teléfono, plan, rol)
- Confirmación → batch via invite-member API → resumen

**Entry points**: Admin card en `/perfil` + Admin link en `/market` header.

## Pendiente
- ~~Deploy a Vercel~~ ✅ Deployado.
- ~~Correr migraciones 00026 + 00027 en SQL Editor del usuario.~~ ✅ Corridas 12/09/2026.
- ~~Establecer `is_admin` para el dueño~~ ✅ `Alla` (overix.digital@gmail.com) con `is_admin=true`.
- ~~Correr migración 00028~~ ✅ Corrida 12/09/2026.
- ~~Redeploy `invite-member`~~ ✅ Redeployada 12/09/2026 (batch + as_admin).

### Cierre de sesión (12/09/2026, 11:40 ART)
- Verificación de 00028: `admin_global_stats` responde `No autorizado` con anon key → guard `_assert_admin` funciona correctamente.
- Cuenta `Alla` (overix.digital@gmail.com, rol gym) creada y seteada como admin.
- Edge function `invite-member` redeployada con soporte batch + `as_admin`.
- Token `sbp_...` de Supabase expuesto en el chat → regenerar/revocar después del deploy.

### Cierre de sesión — Consola Admin (12/09/2026, ~15:00 ART)
- **Objetivo**: cuenta `overix.digital@gmail.com` = solo administrador (no gym), consola estilo empresa para gestionar la plataforma sin código.
- **Decisiones del usuario**: (1) borrar gym de prueba "Lautiadmin", (2) estilo empresa oscuro, (3) soporte CSV + Excel (.xlsx).
- **Commit `646b3f7`**: AdminShell rediseñado (sidebar enterprise full-width), todas las páginas admin en tablas, rol 'admin' en AppRole, root redirect, edge `admin-delete-user`, botón eliminar en /usuarios, importación CSV+Excel en /gyms con `lib/parsePlanilla.ts`, dependencia `xlsx`.
- Edge function `admin-delete-user` deployada (Supabase CLI).
- **Pendiente**: correr migración 00029 (CHECK + role admin + delete gym), deploy Vercel (token expirado — pedir token nuevo).
