# Journal — 20/09/2026

## Contexto
- El usuario confirmó que la **racha + posts cortados del feed** venían con **caché vieja/bundle de Vercel** en el celular: los fixes CSS del Lote 7 estaban deployados, pero el teléfono seguía mostrando el feed recortado.
- Se acordó construir un **medidor v8 dentro del propio feed** (badge diagnóstico) que reporta numéricamente el overflow en el celular real, para saber si es caché o CSS real y, si es CSS, **cuál elemento y cuántos px** desbordan.

## Lote 8 — Medidor v8 en Feed (commit `6b19900`, pusheado)
- **`Feed.tsx`** (una sola definición de cada símbolo — eliminada la copia duplicada que rompía el build):
  - `interface FeedDiag` + `measureDiag(feedEl)` → mide `scrollWidth/clientWidth` de doc y feed + recolecta hasta 3 offenders `{tag, cls, delta}` (scrollWidth > clientWidth, sin el propio feed, dedup por tag+clases).
  - `renderDiag(diag)` → badge fijo bottom (`pointer-events-none fixed inset-x-0 bottom-20 z-[60] flex justify-center`), **autocontenido** con `truncate`/`max-w` para nunca generar overflow él mismo.
  - Root del feed con `ref={feedRef}` y efecto medidor que corre cada 2s + en resize: `measureDiag(feedRef.current)`.
- `npm run build` → **limpio** (`✓ Compiled successfully in 1457ms`, sin errores TS).
- Push OK: `a2547f4..6b19900 main -> main` (branch sincronizado, working tree limpio).

## Lectura del badge v8 en el celular
- Reporte recibido: `v8 · doc 423/423 · feed 544/544 · body.min-h-full.bg-bg +152px | div.min-h-…`.
- **doc 423/423 (dentro de límites)** → la raíz del documento **ya NO desborda** en horizontal: el cursor/recorte del feed quedó resuelto a nivel raíz.
- **feed 544/544 (SW=CW)** → el feed contiene su propio contenido; no hay offender interno con corte visible.
- El único offender es el `body` mismo (+152px), que es el **contenedor** (no un post/card): ningún `post-card`/caption aparece como offender → **el bug original de posts cortados quedó contenida**.
- **Conclusión**: el corte visible previo en el celular era el bundle/caché viejo (Lote 7 ya lo había arreglado); el Lote 8 confirmó por medición que doc y feed no desbordan. Resta la confirmación visual final del usuario en el celular.

## Pendiente
- **Confirma visual en el celular**: si el feed ya se ve entero y la racha visible → cerrar el diagnóstico; si volviera a cortarse con el badge v8 en pantalla, el medidor reporta el offender exacto (`tag.cls +Npx`) para el fix quirúrgico.
- Opcional: sacar el badge v8 del producción una vez confirmado (o dejarlo oculto tras flag) para no ensuciar el feed.
