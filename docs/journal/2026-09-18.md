# Journal — 18/09/2026

## Contexto
- El usuario pidió que la **barra lateral de opciones** no desaparezca al entrar a las áreas web y que las vistas queden **alineadas con la web en PC**.
- Empezamos por **Mis alumnos** (`/entrenamiento`, área del profe): en desktop se perdía la barra lateral y el contenido quedaba "ancho completo, desprolijo".
- Se acordó replicar el patrón `SocialShell` (sidebar fija izq. en desktop + pill inferior en mobile) para el área del profe y luego "lo mismo o parecido" para el **market**.

## Decisiones
- **Patrón unificado de shell responsive** (Training + Market), mismo que ya usa `SocialShell`/`GymShell`/`AdminShell`.
- Perfil del profe: sección pública "Lugares donde trabaja" (gyms vía `gym_staff` autorizado + zonas personalizadas vía `trainer_gyms`) con **mapa Leaflet** (icono cyan gym / icono ember zona) + sección "Alumnos".
- Notificaciones navegan por tipo (`follow` → perfil, `message` → chat, `orden` → mis compras).
- Chat visible en PC: entrada en nav + bandeja `/chat`.

## Implementado
### `TrainingShell` responsivo (commit `3278bce`)
- **`DesktopSidebar`** (`hidden md:flex`, fija izq. `w-64`, mismo estilo SocialShell): brand + badge "Profesor", nav (Alumnos · Mi zona · Market · Perfil), sección "Mi Espacio" (Red social · Editar perfil), **Cerrar sesión**.
- Header y pill del profe → `md:hidden` (solo mobile).
- Root: `<div className="flex min-h-screen bg-bg">` + sidebar + `<div className="flex-1 md:pl-64">{children}</div>`.
- Los 3 mains del training (`/entrenamiento`, `zona`, `alumno/[id]`) de `max-w-full` → `mx-auto max-w-xl md:max-w-2xl lg:max-w-3xl px-4 pt-5`.

### `MarketShell` responsivo (commit `a0a50d1`)
- Mismo patrón para todo el market: sidebar desktop (`hidden md:flex` `w-64`) con nav (Productos · Crear [highlight neón] · Mis ventas · Mis compras · Billetera · Carrito) + **Admin** (solo `is_admin`) + "Mi Espacio" (Red social `/home`) + Cerrar sesión.
- Pill inferior (`md:hidden`) en mobile: Productos · Crear (+) · Mis ventas · Mis compras · Carrito.
- `(market)/layout.tsx`: auth check server-side (`redirect("/login")`) + `MarketShell` (con AuthProvider) — el market ahora **exige iniciar sesión**.
- Los 8 mains del market **ensanchados**: storefront `max-w-md` → `md:max-w-2xl lg:max-w-4xl`; páginas internas (crear/[id]/mis-publicaciones/mis-compras/billetera/carrito) → `md:max-w-2xl lg:max-w-3xl`; quitado `pb-24` (lo maneja el shell).

## Lote 7 — Feed sin overflow + Racha visible (18/09/2026)
- Raiz del bug "no se ve el Racha" en el celular: el feed se desbordaba horizontalmente por su lateral derecho (overflow), el body con `overflow-x-hidden` lo recortaba, y el botón Racha (que cuelga con `ml-auto` al borde derecho de la barra sticky) quedaba fuera de pantalla + las publicaciones salían cortadas por la derecha.
- -> **`Feed.tsx`**: root `w-full` -> `mx-auto max-w-xl w-full overflow-x-clip` (el desborde ahora se recorta UNA capa antes que el body). Barra sticky: agrupada con `shrink-0` en tabs y Racha; Racha con `ml-auto`. Y en **cada chip de cada post (PostCard)** roto el wrap de palabras largas/URLs (`break-words`) que empujaban el ancho.
- Impacto: ahora en mobile los posts se ven enteros y el botón Racha queda visible a la derecha de "Para vos / Siguiendo". (cambios CSS puros, sin lógica)

## Pendiente
- Verificar el deploy responsivo en la web del usuario.
- Racha (feed social): **arreglada** en el Lote 7 (ver arriba) — el desborde lateral recortaba el botón; resta confirmarlo en el celular real.
