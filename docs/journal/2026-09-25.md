# Journal 2026-09-25

## Sesion: Lote 25 cerrado + inventario de migraciones completo (commits f4b859b, f269b61, 19e4153, 3a0ef3b)

## Contexto
El Lote 25 (rebrand SpotterShop + monetizacion por publicaciones, migracion 00036) se implemento el 23-25/09. El usuario corrio la migracion 00036 en el SQL Editor y verificamos por query que TODAS las migraciones 00001-00036 estan aplicadas. Quedo pendiente de cierre: recrear la migracion 00029 (nunca guardada como archivo) y actualizar docs.

## Decisiones
- El listado "private (31)" del SQL Editor de Supabase son BORRADORES guardados, no la DB. Lo que importa es si el SQL se ejecuto: se verifico con un query de marcadores por migracion.
- Query de verificacion (corrido por el usuario): `m29_role_admin`, `m30_read_admin`, `m31_profesor`, `m31_resenas`, `m32_workplaces`, `m32_notifs`, `m33_gyms`, `m33_tgyms`, `m34_zona_feats`, `m35_orden`, `m36_shop`, `m36_config`, `m36_publish`, `m36_rpcs` -> TODOS `true`. Ninguna migracion esta "falta" en la DB.
- Recrear `00029_admin_role.sql` como archivo de registro historico (su contenido: CHECK de role ampliado a 'admin' + UPDATE role='admin' para overix.digital@gmail.com + DELETE gym "Lautiadmin"; la constraint inline de 00001 se llama `profiles_role_check`). NO se vuelve a correr.
- AGENTS.md marca 00030/00031/00032/00033 como CORRIDAS (estaban como pendientes/omisas) y agrega el estado completo 00029-00036; journal 2026-09-23 sesion 5 pasa su pendiente a CORRIDA.

## Implementado
- `supabase/migrations/00029_admin_role.sql` recreado (commit `3a0ef3b`).
- AGENTS.md: migraciones 00029-00036 documentadas como CORRIDAS + nota "todas aplicadas (verificado 25/09/2026)".
- `docs/journal/2026-09-23.md`: pendiente de la sesion 5 marcado como CORRIDA.
- Este journal (2026-09-25).

## Pendiente
- Probar en la app el flujo nuevo: publicar con cupo gratis / publicar con cupo agotado (debita $100) / sin saldo (error + link a billetera), ranking en `/market/admin`, compra sin comision.
- (Opcional, registrado desde antes) alinear `/admin` dashboard global con el nuevo modelo (los KPIs globales de comision siguen mostrando el modelo viejo; las ordenes viejas conservan su snapshot).

## Sesion: Lote 26 - Todo el admin del market en el perfil administrador (commit `f71cbfe`, migracion 00037 PENDIENTE de correr)

## Contexto
El usuario no encontraba `/market/admin` (el panel escondido del market) y pidio que TODO el manejo de dinero/publicadores del marketplace se haga desde el perfil administrador de la app (`/admin`). Eran dos paginas separadas: `/market/admin` (monetizacion, ranking de publicadores, acreditar saldo, retiros) y `/admin/market` (solo KPIs + ordenes + CSV, estilo enterprise).

## Decisiones
- Consolidar TODO en `/admin/market` (la pagina "SpotterShop" del menu del administrador, estilo enterprise).
- `/market/admin` no se elimina: queda como pagina REDIRECT server a `/admin/market` (no 404 para quien pegue la URL).
- Sacar los 2 accesos a `/market/admin`: el escudo "Admin" en el header de `/market` y el item Admin del `MarketShell`.
- Los usuarios no pierden nada: tienda, crear, mis ventas/compras, billetera, carrito y checkout quedan intactos.
- Los retiros NO se listaban bien por RLS (wallet_transactions: el admin solo lee lo propio). Para moverlos al admin de verdad hace falta una RPC `security definer` nueva.

## Implementado
- `supabase/migrations/00037_admin_withdrawals.sql` (NUEVA, PENDIENTE de correr): RPC `admin_pending_withdrawals()` (`security definer`, valida is_admin) = retiros `withdrawal_request` sin su `withdrawal_paid` asociado, con email/username/full_name del usuario. Se reutiliza `admin_mark_withdrawal_paid` (00027) para marcar pagado.
- `src/app/(admin)/admin/market/page.tsx` reescrito: KPIs (Ordenes, Ventas totales, Ventas este mes, Ingresos por publicaciones, Retiros pendientes) + CSV + ultimas ordenes + grafico 6 meses + categorias populares + **Monetizacion SpotterShop** (cupo gratis + precio) + **Publicadores ranking y premios** (stock, stepper cupo, Aplicar, reset Global, buscador) + **Acreditar saldo** + **Retiros pendientes** (boton Pagado). Estilo enterprise.
- `src/app/(market)/market/admin/page.tsx` -> redirect server a `/admin/market`.
- `src/app/(market)/market/page.tsx`: quitado el escudo Admin (y imports/state `profile`/`Shield`/`useAuthState` que quedaban sin uso).
- `src/components/market/MarketShell.tsx`: quitado el item Admin (y `Shield`/`useAuthState` sin uso).
- Lint 0 errores, build OK (44 rutas; `/market/admin` sigue existiendo como redirect).

## Pendiente
- **Usuario: correr `00037_admin_withdrawals.sql` en el SQL Editor** (se pega el SQL al final del chat).
- Probar: la consola `/admin/market` con monetizacion/publicadores/acreditar/retiros; `/market/admin` redirige solo.