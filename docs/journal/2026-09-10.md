# 2026-09-10

## Contexto
- Continuamos con el plan pulido: quedaban pendientes del día anterior "Alumno busca profe por zona con mapa (Leaflet)" y "Adjuntos en chat". Ambos se resolvieron hoy.
- Además se cargó un token nuevo de Vercel (el anterior había expirado).

## Decisiones
- **Buscar profe = solo contacto y mensaje** (sin tabla de solicitudes ni migración): el chat 1:1 ya funciona con cualquier usuario sin chat previo, así que el alumno contacta directo desde el mapa.
- **Adjuntos en storage SIN tocar policies**: la policy de insert del bucket `media` exige que el primer folder del path sea `auth.uid()`; por eso los adjuntos se suben a `media/<userId>/chat/<timestamp>.<ext>` (no `chat/<uid>/`, que la policy rechazaría).
- **`messages.content` pasa a permitir NULL**: un mensaje puede ser solo imagen (el texto queda opcional).

## Implementado
- **Buscar profe por zona** (commit `809ba0c`, docs `f377c98`, deploy): página nueva `/mi-entrenamiento/buscar` con mapa **Leaflet** multi-marcador (icono ember reutilizando patrón de `GymMap`) + buscador (ciudad/dirección/zona/nombre) + lista de profes con badge "Ya te entrena" (`trainer_students` activos). **Ver perfil** (`/perfil/<username>`) y **Mensaje** (`/chat/<trainerId>`). Botón "Mi zona" (geolocalización, default Argentina zoom 5). Datos: `trainer_gyms` (público) + `profiles` en consulta separada con `Map` (lección de embeds). Entradas: header + empty state de `/mi-entrenamiento` y card en `/perfil` (rol alumno). 27 rutas OK.
- **Token Vercel nuevo** (commit `1da5b86`): `vcp_1ogPklp...` cargado en AGENTS.md.
- **Adjuntos en el chat** (commit `ebe091f`, docs `48c196e`, deploy): migración **`00024_chat_attachments.sql`** (columnas `messages.attachment jsonb` + `content` nullable). UI en ambos chats (`/chat/[id]` y pestaña Chat de `/mi-entrenamiento`): botón 📎 con input `capture="environment"` (image/video), preview removible, texto opcional, spinner, y burbujas con imagen + texto. Subida a `media/<userId>/chat/...`. Realtime ya cubierto (00013).

## Pendiente
- ~~Correr migración 00024~~ ➜ **CORRIDA 10/09/2026**, verificada: `attachment jsonb` presente y `content` nullable en `messages`.
- Plan maestro: continuar con siguiente ítem del roadmap del plan.

---

## Historial de rutinas: calendario + racha + cumplimiento + publicar en feed (hecho, commit `3aae915`)

## Contexto
- Siguiente ítem del plan maestro: "Calendario/gráfico de historial de rutinas" (pendiente del journal 09/09). El alumno ya tenía progreso por ejercicio; faltaba la vista global.

## Decisiones
- **Nueva pestaña "Historial"** (4ª) en el alumno **y** en el profe (el usuario eligió "alumno y profe").
- **Incluir racha 🔥 + stats** y un **botón "Publicar en el feed"** por sesión (solo alumno).
- **Sin migración**: `trainer_routine_logs` ya tiene policy "profe lee" (00016) y `posts` acepta posts solo-texto. Sin dependencias nuevas (recharts ya estaba).

## Implementado
- **`src/lib/history.ts`** (nuevo): helpers puros `monthGrid/monthLabel/isFutureDate/countLogsByDate/sessionsByDate/plannedDates/computeStreak/monthlySeries/categoryForDiscipline`.
- **Alumno `/mi-entrenamiento`**: tab Historial con calendario mensual (intensidad por sesiones, planificadas con borde ember, hoy con anillo, navegación sin futuro), stats (días/racha/top mes), BarChart 6 meses hechas vs planificadas, bottom sheet por día con "Publicar en el feed" (caption `🔥 Terminé "…" · Día N`, categoría mapeada, link a `/posts/[id]`), realtime de `trainer_routine_logs` agregado.
- **Profe `/entrenamiento/alumno/[id]`**: carga los logs del alumno, mismo tab Historial (solo lectura, sin publicar), realtime agregado.

## Pendiente
- Probar publicar una sesión desde el calendario y ver el post en el feed.
- Plan maestro: continuar con siguiente ítem del roadmap del plan.